# Folder to TXT 合并导出

把一个文件夹里的 md 合并成一个 txt，自动补章节标题。

## 用法

长按文件夹 → 「合并文件夹为 TXT」

或命令面板搜「合并文件夹为 TXT」。

## 输出

```
第一章 惊变

（正文……）

第二章 入局

（正文……）
```

文件名取文件夹名，存在同一层。

## 注意

- 按文件名排序，建议用 `第001章` 这种带前导零的命名
- 会跳过 YAML frontmatter
- 被书籍写作、创作工作台复用为导出引擎

## 命令

- 合并文件夹为 TXT

## 依赖

Mogeo Core
