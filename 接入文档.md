# Mogeo 插件接入文档

## 给新插件：三行搞定

```js
// ① 把 mogeo.js 的内容内联进 main.js（插件目录只留 main.js / manifest.json）
// ② 一行接入
async onload() {
  const M = Mogeo.boot(this, {
    id:   'my-plugin',
    name: '我的插件',
    desc: '一句话说明',
  });
  // ③ 不要 return！Core 没启用时 M 是 null，但命令照常注册
  //    提示推迟到用户真的点命令时（guard），避免开 Obsidian 就刷屏
}
```

> ⚠️ **不要用 `require('./mogeo')`** —— Obsidian 移动端不保证支持相对路径
> 文件 require，插件会加载失败。必须内联。

**就这样。** Core 启用了就能用，没启用就用不了 —— SDK 自动处理提示和跳转设置页。

## 命令级守卫

```js
this.addCommand({
  id: 'do-something',
  name: '干活',
  callback: () => Mogeo.guard(this, () => this.doSomething()),
});
```

## 用别的能力

```js
const tts = Mogeo.use(this, 'tts');   // 拿朗读能力
if (tts) tts.speak('你好');

Mogeo.provide(this, 'my-cap', api, { name: '我的能力' });  // 提供能力
```

## 检查 Core 状态（不弹提示）

```js
Mogeo.alive(this);   // → true / false
```

## 文件说明

| 文件 | 作用 |
|---|---|
| `mogeo.js` | SDK，拷进每个插件目录，**内容永远不变** |
| Core | 真正的实现，升级能力不需要改 SDK |

`mogeo.js` 是薄壳：优先用 Core 挂到 `globalThis.Mogeo` 的实现；
Core 比本插件晚加载时，回退到直接查 `app.plugins.plugins['ai-toolkit-core']`。

## 为什么这样设计

- **SDK 永远不改** —— 新能力加在 Core 里，所有插件自动受益
- **找不到 Core 也安全** —— 弹提示、跳设置页、返回 null，不崩
- **自动登记** —— 调了 boot 就在 Core 面板的套件列表里出现，Core 不用改

## 现有插件的接入位置

六个插件都在 `onload()` 里调 `Mogeo.boot(this, {...})`，
`tts-doctor` 的三个命令额外用 `Mogeo.guard()` 包了。

作者统一为 **Mogeo**。


---

# v2.0.1：不再一打开就弹提示

## 问题

`boot()` 在 `onload()` 里被调用，而 `onload` 每次开 Obsidian 都会跑。
七个插件 × 每次启动 = 一打开就刷一屏「需要启用 Mogeo Core」。

## 改法

`boot()` 改成**静默**：Core 不在时直接返回 `null`，不弹任何提示。

提示推迟到 `guard()` —— 用户真的点命令时才说。

配套改动：插件 `onload` 里**不要** `if (!M) return`，
命令照常注册（用 `coreGuard` / `Mogeo.guard` 包住），
这样 Core 没启用时点命令会提示，而不是命令压根不存在。

## 名称统一

Core 的显示名从「AI 工具箱 Core」改成 **Mogeo Core**，
六个插件里硬编码的中文名也一并改了。
