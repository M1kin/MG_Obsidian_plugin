# GitHub 账号注册与 Token 申请

给**小说同步**插件用的完整流程。从零开始，手机端也能做完。

全程约 5 分钟。

---

## 目录

- [一、注册账号](#一注册账号)
- [二、建一个私有仓库](#二建一个私有仓库)
- [三、申请 Token（重点）](#三申请-token重点)
- [四、填进插件](#四填进插件)
- [五、常见错误](#五常见错误)
- [六、安全提醒](#六安全提醒)

---

## 一、注册账号

### 1. 打开注册页

手机浏览器打开：

```
https://github.com/signup
```

### 2. 填邮箱

用一个**常用且能收到邮件**的邮箱。QQ、163、Gmail 都行。

填完点 **Continue**。

### 3. 设密码

至少 8 位，别用纯数字。

### 4. 取用户名

这个会变成你的主页地址：

```
github.com/你的用户名
```

⚠️ **建议一次取好**。虽然能改，但改了之后旧链接会失效，已经配好的插件也要重填。

### 5. 邮箱验证

GitHub 会发一封验证邮件。

**收不到？**

- 看垃圾邮件箱
- 等 2 分钟再刷新
- QQ 邮箱可能被拦，换 Gmail / Outlook 更稳

**一定要验证完再继续**，不然建不了仓库。

### 6. 人机验证

会让你做几次拼图/选择题，照着做就行。

---

## 二、建一个私有仓库

仓库就是"放你小说的地方"。

### 1. 打开新建页

```
https://github.com/new
```

或者点右上角头像 → **Your repositories** → 绿色 **New**。

### 2. 填信息

| 项 | 填什么 |
|---|---|
| **Repository name** | `novel-sync`（随便起，记住它） |
| **Description** | 选填，写"小说备份"就行 |
| **⚠️ Public / Private** | **选 Private（私有）** |
| Add a README file | **不勾** |
| Add .gitignore | **不勾** |
| Choose a license | **不勾** |

### 三个都不勾的理由

插件第一次同步会自己建文件。如果仓库里已经有 README，可能产生冲突。

**空白仓库最省事。**

### 3. 点 Create repository

建好后地址长这样：

```
https://github.com/你的用户名/novel-sync
```

**记下用户名和仓库名**，下一步要用。

---

## 三、申请 Token（重点）

Token 就是"插件访问你仓库的钥匙"。

### 1. 打开申请页

```
https://github.com/settings/tokens/new
```

（也可以：头像 → Settings → 最下面 Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic)）

### 2. 起个名字

**Note** 填：

```
小说同步
```

方便以后知道这把钥匙是干什么的。

### 3. 选有效期

**Expiration** 选：

| 选项 | 建议 |
|---|---|
| 30 days | 够用，但要记得续 |
| 90 days | **推荐** —— 省心又不会永久暴露 |
| No expiration | ❌ 别选，丢了就永久泄露 |

### 4. 勾权限（最关键）

页面往下拉，看到 **Select scopes** 一长串复选框。

**只勾第一个：**

```
☑ repo    Full control of private repositories
```

其余全部**不勾**：

```
☐ workflow
☐ write:packages
☐ delete:packages
☐ admin:org
☐ admin:public_key
☐ write:discussion
☐ ...（下面还有几个，全都不动）
```

> **为什么是 `repo`**：它同时管公有和私有仓库的读写。
> 我们要的"上传文件"就在里面。
>
> 虽然描述写着 private repositories，但它也包含 public 仓库的写权限——
> 所以给多了，但没法更细。用有效期和删除来控制风险。

### 5. 生成

拉到最底部，点绿色 **Generate token**。

### 6. ⚠️ 复制并保存

页面会显示一串字符：

```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**只显示这一次！** 关掉页面就再也看不到了（只能重新生成）。

现在就**长按复制**，粘到备忘录里。

---

## 四、填进插件

### 1. 打开插件设置

Obsidian → 设置 → 第三方插件 → **小说同步** → 齿轮图标

### 2. 填四项

| 项 | 填什么 | 例子 |
|---|---|---|
| **GitHub Token** | 刚才那串 `ghp_` | `ghp_xxxx...` |
| **用户名** | 你的 GitHub 用户名 | `M1kin` |
| **仓库名** | 刚才建的仓库 | `novel-sync` |
| **分支** | 一般填 `main` | `main` |

> 分支怎么确认？仓库页面左上角如果显示 `main` 就填 `main`，
> 显示 `master` 就填 `master`。

### 3. 点「测试连接」

设置页有个「测试连接」按钮，**一定先点一下**。

| 结果 | 说明 |
|---|---|
| ✅ 连上了：xxx/novel-sync（私有） | 成功，可以开始同步 |
| ❌ Token 无效或已过期 | 复制错了，或过期了 |
| ❌ 仓库不存在，或 Token 没权限 | 用户名/仓库名写错，或没勾 `repo` |

### 4. 设同步范围

设置页「要备份的文件夹」，一行一个：

```
小说相关
设定集
```

**留空 = 整个 vault。**

「忽略规则」默认已经排除了 `.obsidian`，不用改。

### 5. 开始同步

- **左侧栏**点「立即备份」
- 或**长按文件夹** → 「📤 同步此文件夹到 GitHub」

---

## 五、常见错误

### Token 无效或已过期（401）

- 复制时多了空格？检查首尾
- 是不是选了 No expiration 以外的短时效，已经过期
- 重新生成一个

### 仓库不存在，或 Token 没权限（404）

- 用户名/仓库名拼写错了
- 仓库是别人的（你没权限）
- **Token 忘了勾 `repo`** ← 最常见

### 被限流了（403）

同步太频繁。等几分钟再试。

正常写作用不到限流阈值。

### 同步很慢

第一次要传所有文件，慢是正常的。

之后只传变过的，会很快。文件多的话缩小同步范围。

### 手机上打不开 GitHub

如果 `github.com` 访问困难，需要自备网络环境。

---

## 六、安全提醒

### ⚠️ Token = 你所有仓库的钥匙

勾了 `repo` 的 token 能读写你**名下所有仓库**（包括私有的）。

所以：

**1. 用完就删**

如果不再用了，立刻去：

```
https://github.com/settings/tokens
```

点 token 右边的 **Delete**。

**2. 别发给别人**

任何人拿到这串字符都能操作你的仓库。

**3. 定期换**

建议 90 天换一次。到期前 GitHub 会发邮件提醒。

**4. 别把 vault 公开上传**

Token 存在插件的 `data.json` 里（明文）。

如果你把整个 Obsidian 库传到公开地方，等于把钥匙也传了。

---

## 快速回顾

```
1. github.com/signup        注册 + 验证邮箱
2. github.com/new           建私有仓库（三个勾都不选）
3. github.com/settings/tokens/new
                            勾 repo，有效期 90 天，生成
4. 复制 ghp_ 开头的那串      ← 只显示一次！
5. 插件设置填 4 项 → 测试连接
6. 左侧栏「立即备份」
```

---

有问题？把「测试连接」的错误提示截图发出来，一眼就能定位。
